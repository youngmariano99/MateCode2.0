# Sprint 23: Ajustes de Entrenamiento + Planificación (grupos A y B)

> Seguimiento vivo. El usuario planteó una lista grande de consideraciones tras probar Entrenamiento y Planificación (ex "Hoy") en vivo — se agrupó en A (chico), B (extiende lo que ya existe) y C+D ("Oficina", subsistema nuevo, se planifica aparte más adelante).

## Checklist de ejecución

### Grupo A — arreglos chicos ✅ COMPLETO

- [x] Bug "1kg" en ejercicios de peso corporal: `armarEstructura` en `ImportarBloqueEntrenamientoUseCase` ahora descarta `pesoKg` si el ejercicio resuelto tiene `permiteCarga: false`, sin importar lo que haya mandado la IA — defensa server-side, no depende de que el prompt se respete. Prompts (`generarPromptRutina`/`generarPromptBloqueCompleto`) actualizados con `NOTA_PESO_CORPORAL` + ejemplo JSON corregido (antes mostraba `"pesoKg": null` en todos los ejercicios, confundía a la IA).
- [x] Menú lateral: "Hoy" → "Planificación" (`menu-items.ts` + breadcrumb en `hoy/page.tsx`) — la ruta `/dashboard/personal/hoy` no cambió, solo la etiqueta.
- [x] Soft-delete de Bloques: `BloqueEntrenamiento.eliminado: boolean` (nuevo campo), `GestionarBloquesUseCase.eliminarBloque(id)` nunca toca `RegistroActividad`, todas las queries de bloques activos/planificados filtran `!eliminado`. `ConfirmarEliminarBloqueModal` (nuevo) muestra el conteo real de sesiones antes de confirmar (mismo criterio que `ConfirmarEliminacionNodo` en Personal). Postgres: columna `eliminado` aplicada en vivo.
- [x] Tests nuevos: pesoKg nunca se guarda en ejercicio sin `permiteCarga` aunque el JSON lo traiga; eliminar un bloque con sesiones reales las deja intactas.
- [x] Verificación: typecheck / eslint / test (194/194) / build — todo limpio

### Grupo B — extender lo que ya existe

- [x] Modelo de datos: `BloqueEntrenamiento.plantillaIds: string[]` reemplazado por `rutinasProgramadas: RutinaProgramada[]` (`{ plantillaId, diasSemana }`, 0=domingo...6=sábado) — mismo patrón de recurrencia semanal que `RecurrenciaEntregable` en Personal. Confirmado con el usuario ("Días de la semana fijos por rutina"). `aplicaHoyRutina(diasSemana, diaISO)` nueva función pura en `rutina.entity.ts`. Postgres: columna `plantilla_ids` dropeada (sin datos reales), `rutinas_programadas jsonb` agregada en vivo.
- [x] `GestionarBloquesUseCase.vincularRutinas` reemplazado por `programarRutina(bloqueId, plantillaId, diasSemana)` (upsert — actualiza días si ya estaba programada) y `quitarRutinaDelBloque(bloqueId, plantillaId)` (nunca toca `RegistroActividad`).
- [x] `ImportarBloqueEntrenamientoUseCase.importarBloqueCompleto` arma `rutinasProgramadas` desde el JSON (`diasSemana` por rutina, default lunes-viernes si la IA lo omite). Prompt (`generarPromptBloqueCompleto`) actualizado para pedir `diasSemana` explícito. Preview (`resumenBloqueCompleto`) muestra los días de cada rutina.
- [x] `panel-bloques.tsx`: UI de vinculación manual gana selector de días (toggle L-M-M-J-V-S-D), lista de rutinas programadas muestra días + botón para quitar.
- [x] **Calendario semanal de Entrenamiento** (`calendario-entrenamiento.tsx`, nuevo) — 7 columnas, por día muestra qué Rutinas tocan según `rutinasProgramadas` del bloque activo (vía `aplicaHoyRutina`) y si ya hay `RegistroActividad` para ese día (tachado + "Marcar hecha" si no). Montado en la estación "Hoy" de `entrenamiento/page.tsx`, entre `PanelBloques` y `EjecucionSesion`. Esto resuelve tanto "vista para ver el bloque y las rutinas que tengo que hacer" como "ver cuáles hice en la semana".
- [x] Verificación: typecheck / eslint / test (194/194) / build — todo limpio
- [x] Avance parcial: `AvanceParcial` (nuevo, dentro de `bunker-del-dia.tsx`) — en cualquier Actividad con `cantidadObjetivo` declarado aparece "progresoActual/cantidadObjetivo unidad" + input numérico + botón "+" que llama a `GestionarActividadesUseCase.registrarAvance(id, cantidad)` (ya existía, ya acumula hacia Entregable/Proyecto/Objetivo). No reemplaza "Completar" — conviven.
- [x] Historial de pausas activas: `panel-pausas-activas.tsx` — cada chip de rutina gana un botón "Marcar hecha hoy" (`registrarComoPlanificado(plantillaId, hoy)`, sin `bloqueId`), y una sección "Hechas esta semana" lista los `RegistroActividad` de los últimos 7 días sin `bloqueId` (o sea, pausas activas, no sesiones de bloque).
- [x] Verificación: typecheck / eslint / test (194/194) / build — todo limpio. **No verificado visualmente en browser** — la app pide login y no tengo la contraseña del usuario (entrar credenciales está fuera de lo que puedo hacer).
- [ ] Confirmar con el usuario que el sistema de Fases (Sprint 21) ya resuelve "la IA calcula la progresión para llegar a 250 contactos" — puede que solo haga falta acomodarlo mejor en el flujo principal, no rehacer nada

**Nota de diseño no confirmada con el usuario todavía:** "mover una rutina de un día a otro" no se implementó como una reprogramación con estado — `rutinasProgramadas.diasSemana` es el patrón semanal estable, y registrar una sesión en un día distinto al patrón (vía `registrarComoPlanificado(plantillaId, diaTarea, bloqueId)`, que ya acepta cualquier `diaTarea`) simplemente queda registrada ese día. A diferencia de Personal (`Actividad.migrarActividad`), acá no hay una instancia materializada por día que migrar — las Rutinas-en-un-Bloque no se materializan, así que no hace falta un concepto de "excepción". Si el usuario esperaba algo más explícito (ej. "hoy no puedo, la paso a mañana" como acción concreta), se puede agregar un botón puntual sobre el calendario más adelante.

### Grupo C+D — "Oficina" ✅ COMPLETO

Nuevo lugar `/dashboard/personal/oficina` — la pantalla que se mantiene abierta mientras se trabaja, distinta de "Planificación" (el ritual de revisión/armado del día).

- [x] **Cronómetro por tarea**: `SesionTrabajo` (nueva entidad, `sesion-trabajo.entity.ts`) + `GestionarSesionTrabajoUseCase` (nuevo) — `iniciarSesion`/`pausarParaDescanso`/`reanudarSesion`/`finalizarSesion`. Persistida en Dexie (tabla nueva `sesion_trabajo`, versión 27, sin migración de datos previos), no solo estado de React — sobrevive a un reload o a navegar a otro módulo por los accesos directos. Una sola sesión `activa`/`pausada` a la vez, app-wide (mismo criterio de foco único que `MAX_TAREAS_ENFOQUE_POR_DIA=1`) — `iniciarSesion` lo rechaza explícitamente si ya hay otra en curso. Finalizar una sesión NUNCA completa la Actividad — son acciones separadas a propósito.
- [x] **Interrupción con pausa activa**: pausar la sesión (`pausarParaDescanso`) acumula el tiempo corrido y muestra `PausaActivaRapida` (elegir una rutina de pausa activa o "Al azar", o "Volver sin pausa activa") — al elegir una, la registra (`registrarComoPlanificado`, reusado de Entrenamiento) y reanuda el cronómetro.
- [x] **Anotador de ideas**: no se construyó nada nuevo — la Bandeja de entrada (`CapturaInbox`, ya existía) es exactamente esto (captura libre + triage después), se reusa tal cual en Oficina.
- [x] **Accesos directos**: `AccesosRapidos` (nuevo) — links a Contacto en Frío, Contenido y Desarrollo (Proyectos), cruzando a propósito el switcher Profesional/Personal.
- [x] **"1 prioridad + 3 mantenimiento + extras"**: resuelto como agrupación visual en `ListaParaComenzar` (dentro de `sesion-trabajo.tsx`), reusando `MAX_TAREAS_ENFOQUE_POR_DIA`/`MAX_TAREAS_MANTENIMIENTO_POR_DIA` ya existentes — sin tocar ningún prompt de IA ni agregar campos nuevos a `Actividad`.
- [x] Postgres: tabla `sesion_trabajo` aplicada en vivo (script temporal, borrado después), agregada al `tableMapper` y a `dateFields` (`iniciadoEn`/`pausadoEn`) en ambas rutas de sync.
- [x] Tests nuevos (`sesion-trabajo.test.ts`, 10 casos): guard de sesión única (activa y pausada), guard de actividad no-pendiente, acumulación correcta al pausar/reanudar/finalizar, finalizar nunca toca la Actividad, el guard no alcanza a sesiones `finalizada`.
- [x] Verificación: typecheck / eslint / test (204/204) / build — todo limpio. **No verificado visualmente en browser** (mismo límite que el resto del sprint — la app pide login).

### Grupo E — integración de Fases + UX de tareas ✅ COMPLETO

- [x] **`FasesActivas`** (nuevo, `oficina/fases-activas.tsx`, montado en Oficina): muestra cada Fase abierta con `entregable — título`, progreso, badge de ritmo (Cumplido/Vencido/Al día/Atrasado/Adelantado) y "necesitás X/día para llegar" — reusa `calcularRitmoObjetivo` tal cual (mismo cálculo que `WidgetObjetivo`, `FasePersonal` tiene el mismo shape `cantidadObjetivo/progresoActual/diaInicio/diaLimite`). No reemplaza el aviso de Fases vencidas en Planificación (ese resuelve el cierre); esto solo informa el ritmo mientras se trabaja.
- [x] **UX de tareas en `bunker-del-dia.tsx`** (rediseño, no cambia el modelo de datos): las tareas resueltas (Hecha/Cancelada/Pasó a mañana) ya NO desaparecen — la query de hoy pasó a traer todos los estados (antes solo `"pendiente"`), y se separan en `pendientesHoy` (arriba, con acciones) y `resueltasHoy` (nueva sección "Resueltas hoy" al final, tachadas + badge de qué pasó, ordenadas por más reciente). Esto resuelve directamente la confusión de "¿la elimino o la marco como no hecha?": cancelar deja la tarea visible abajo marcada "Cancelada", nunca borra nada.
- [x] Los 3 botones de acción (antes solo íconos) ahora llevan texto visible — "Hecha"/"Mañana"/"Cancelar" — mismo patrón de pill-button ya usado en `captura-inbox.tsx` (`min-h-11`, texto uppercase chico), para no depender de que alguien adivine qué hace cada ícono ni de un tooltip que en mobile no se ve.
- [x] Verificación: typecheck / eslint / test (204/204, sin tests nuevos — cambio de UI/agrupación, sin lógica de dominio nueva) / build — todo limpio. **No verificado visualmente en browser** (mismo límite — la app pide login).
